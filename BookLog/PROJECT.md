# 책장(BookShelf) — 기획서

## 1. 목적
- 한 줄: 내가 읽은/읽을 책을 기록하고, 독서 통계를 차트로 보고, AI로 다음 책을 추천받는 개인용 웹앱.
- 만드는 이유(포트폴리오 관점): 데이터 시각화(차트) + 검색/상태관리 + AI 통합을 한 프로젝트에서 보여준다.
- 사용자: 나 자신(책 읽는 사람). 처음엔 1인용으로 시작, 이후 로그인 추가.

## 2. 기능 범위 (MVP를 먼저, 나머지는 나중)

### MVP (가장 먼저 — 이것만 돼도 배포 가능)
- 책 검색: 제목/저자로 검색해서 결과를 카드로 표시 (알라딘 API)
- 서재에 담기: 검색 결과를 '읽고싶어요' 상태로 내 서재에 추가
- 내 서재 목록: 상태별(읽고싶어요/읽는중/완독) 탭으로 보기, 상태 변경

### v2 (기록 강화)
- 별점(0~5), 한 줄 메모, 시작일/완독일 입력
- 책 상세 페이지

### v3 (통계 대시보드) — 나의 강점 어필 지점
- 월별 완독 권수 (라인/바 차트)
- 장르 분포 (도넛 차트)
- 누적 페이지 수, 평균 평점, 올해 목표 달성률
- 책 리스트에서 책 제목, 작가, 출판사 리스트를 텍스트로 복사

### v4 (AI 추천) — 차별화
- 자연어 입력("요즘 마음이 복잡한데 위로되는 소설") → LLM 추천
- 내 서재(읽은 책) 기반 "취향 분석 + 다음 책 추천"
- 추천 결과를 알라딘 API로 실제 표지/링크와 함께 카드로 표시

### v5 (제품화 마무리)
- 로그인(Supabase Auth) + DB 영구 저장(Supabase Postgres)
- 반응형/다크모드, 배포(Vercel), README 정리

## 3. 화면(라우트)
| 경로 | 화면 | 단계 |
|------|------|------|
| `/search` | 책 검색 → 서재 담기 | MVP |
| `/shelf` | 내 서재(상태별 탭, 상태 변경) | MVP |
| `/book/[isbn]` | 책 상세 + 기록(별점/메모) 편집 | v2 |
| `/stats` | 통계 대시보드 | v3 |
| `/recommend` | AI 추천 | v4 |

## 4. 데이터 모델
MVP는 localStorage로 시작해도 되고, v5에서 Supabase 테이블로 이전한다.

**books** (책 기본 정보 — 알라딘에서 받아 캐싱)
- `isbn` (PK), `title`, `author`, `publisher`, `coverUrl`, `pageCount`, `category`, `pubDate`

**shelfItems** (내 기록)
- `id`, `isbn`(→books), `status`('want'|'reading'|'done'),
- `rating`(0~5), `memo`, `startedAt`, `finishedAt`, `createdAt`
- (v5) `userId`

## 5. 외부 API
- **알라딘 OpenAPI**: 책 검색(ItemSearch), 상세(ItemLookUp). `ttbkey` 필요, 서버 경유.
  - 검색: `ItemSearch.aspx?ttbkey=...&Query=검색어&QueryType=Keyword&output=js&Version=20131101`
- **LLM API**: 추천/취향 분석. 서버 Route Handler에서 호출, 키는 서버에만.
- (v5) **Supabase**: Postgres + Auth.

## 6. AI 기능 설계 (v4)
- 입력: 사용자의 자연어 문장 + (선택) 내 서재 목록 요약
- 처리: 서버 `/api/recommend` 에서 LLM에 "조건에 맞는 책 3~5권을 제목/저자/이유로 JSON만 응답" 형식으로 요청
- 후처리: 받은 제목으로 알라딘 검색 → 실제 표지/링크 매칭 → 카드로 표시
- 주의: LLM이 실제로 없는 책을 지어낼 수 있으니, 알라딘에서 매칭 안 되면 카드에서 제외하거나 표시.

## 7. 성공 기준
- 배포된 URL이 있고, 검색→기록→통계→AI추천이 끊김 없이 동작
- README에 스크린샷 + 기술 선택 이유 + 트러블슈팅(특히 상태관리/차트/AI 매칭) 정리
- 이력서 한 줄: "독서 기록·통계 대시보드 + AI 추천 (Next.js·TS·TanStack Query·Recharts) — [배포][깃헙]"
